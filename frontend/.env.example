# Bandung AI Travel Agent — frontend env template
#
# CRA exposes only variables prefixed with REACT_APP_ at build time.
# Copy this file to .env.development (local) or set in Vercel dashboard (prod).

REACT_APP_API_URL=http://localhost:8000
